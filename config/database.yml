default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: trinity_tag_development

test:
  <<: *default
  database: trinity_tag_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
